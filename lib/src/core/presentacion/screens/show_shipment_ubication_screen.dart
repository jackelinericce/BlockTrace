import 'package:flutter/material.dart';

class ShowShipmentUbicationScreen extends StatelessWidget {
  const ShowShipmentUbicationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ruta'),
      ),
      body: Container(),
    );
  }
}
